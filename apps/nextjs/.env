# Next.js Environment Variables
# This file contains default values for all environments
# Override these in .env.local (not committed), .env.development/.env.production, or via external environment variables (e.g. Kubernetes)

# Auth Service URL (backend API, server-side only)
AUTH_SERVICE_URL=http://localhost:4000

# Public API URL (for client-side API calls)
PUBLIC_API_URL=http://localhost:4000/api

# Application URL (frontend)
APP_URL=http://localhost:3000

# Phoenix WebSocket URL (for real-time connections)
PUBLIC_CHANNEL_URL=ws://localhost:4001/socket

# JWT Token Signing Secret (must match backend TOKEN_SIGNING_SECRET)
# In production, this should be set via environment variable
TOKEN_SIGNING_SECRET=MfFey7vLAONsdSzoD0/8F+/jSLzJoSPj