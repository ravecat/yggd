# Google OAuth Configuration
# Get these credentials from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials

GOOGLE_CLIENT_ID=your_client_id_from_google_cloud_console
GOOGLE_CLIENT_SECRET=your_client_secret_from_google_cloud_console
GOOGLE_REDIRECT_URI=http://localhost:4000/auth/user/google/callback

# Federated Authentication Redirect Whitelist
# Comma-separated list of allowed redirect URIs for federated auth callbacks
# Supports wildcards:
#   - localhost:* matches any port (e.g., localhost:3000, localhost:4200)
#   - *.example.com matches any subdomain
# Examples: localhost:*,*.ngrok.io,app.mycompany.com
FEDERATED_AUTH_REDIRECT_WHITELIST=localhost:*,*.ngrok.io

