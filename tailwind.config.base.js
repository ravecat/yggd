/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        // Add shared color palette here
      },
      fontFamily: {
        // Add shared fonts here
      },
      spacing: {
        // Add shared spacing scale here
      },
    },
  },
  plugins: [
    // Add shared plugins here
  ],
};
